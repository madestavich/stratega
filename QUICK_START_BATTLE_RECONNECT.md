# 🚀 Quick Start: Battle Reconnect System

## Швидке впровадження (5 хвилин)

### Крок 1: Застосувати міграцію БД ⚡

Виберіть один із варіантів:

**Варіант A - Через phpMyAdmin:**

1. Відкрийте phpMyAdmin
2. Виберіть вашу базу даних
3. Вкладка "SQL"
4. Скопіюйте вміст `server/migrations/add_battle_state.sql`
5. Натисніть "Вперед"

**Варіант B - Через командний рядок:**

```bash
cd server/migrations
mysql -u your_username -p your_database < add_battle_state.sql
```

**Варіант C - Через Windows скрипт:**

```bash
cd server/migrations
apply_migration.bat
```

### Крок 2: Перевірка ✅

Запустіть в MySQL:

```sql
SHOW COLUMNS FROM game_rooms WHERE Field LIKE '%battle%';
```

Ви повинні побачити:

- battle_started
- player1_in_battle
- player2_in_battle
- battle_winner_id
- last_round_winner_id

### Крок 3: Готово! 🎉

Система вже працює! Можете тестувати.

## Швидкий тест:

1. Створіть кімнату
2. Зайдіть двома гравцями
3. Натисніть "ГОТОВИЙ" обом
4. Під час бою спробуйте F5 → побачите попередження
5. Підтвердіть оновлення → побачите екран очікування
6. Другий гравець завершує бій → перший побачить результат

## Що далі?

Детальне тестування: `BATTLE_RECONNECT_TEST.md`
Повний опис змін: `BATTLE_RECONNECT_SUMMARY.md`

---

**Питання?** Перевірте консоль браузера (F12) на помилки.
